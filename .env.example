# Copy to .env.local and fill in. .env.local is gitignored.

# Resend API key - https://resend.com/api-keys
# Free tier: 3,000 emails/month, no domain and no card required.
# While using the shared onboarding@resend.dev sender, mail can only be
# delivered to the address that owns this Resend account.
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxx

# TEMPORARY override for where contact form submissions are delivered.
# Normally you do NOT set this - the route defaults to the contact@dkaylabs.com
# Zoho group, which is where submissions belong.
#
# It only exists because the shared onboarding@resend.dev sender refuses to
# deliver anywhere except the Resend account owner's address. Until
# send.dkaylabs.com is verified in Resend, set this to that owner address so
# the live form keeps working, then DELETE it (here and in Vercel) once the
# domain is verified.
# CONTACT_INBOX=owner-address@example.com

# Canonical origin, no trailing slash. Only needed if this deploy is NOT the
# production domain - preview builds should set their own URL so canonicals
# and OG tags don't point at production. Defaults to https://dkaylabs.com.
# NEXT_PUBLIC_SITE_URL=https://dkaylabs.com
